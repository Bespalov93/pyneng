# -*- coding: utf-8 -*-
"""
Задание 6.2b

Сделать копию скрипта задания 6.2a.

Дополнить скрипт: Если адрес был введен неправильно, запросить адрес снова.

Если адрес задан неправильно, выводить сообщение: 'Неправильный IP-адрес'
Сообщение "Неправильный IP-адрес" должно выводиться только один раз,
даже если несколько пунктов выше не выполнены.

Ограничение: Все задания надо выполнять используя только пройденные темы.
"""
while True:
    ip = input('Введите IP-адрес в формате 10.0.1.1: ')
    octets = ip.split('.')
    
    # Проверка специальных адресов
    if ip == '255.255.255.255':
        print('local broadcast')
        break
    elif ip == '0.0.0.0':
        print('unassigned')
        break
    # Проверка, что IP-адрес состоит из 4 октетов и каждый октет — это число от 0 до 255
    elif len(octets) == 4 and all(octet.isdigit() and 0 <= int(octet) <= 255 for octet in octets):
        first_octet = int(octets[0])

        # Проверка диапазонов
        if 1 <= first_octet <= 223:
            print('unicast')
        elif 224 <= first_octet <= 239:
            print('multicast')
        else:
            print('unused')
        break  # Выход из цикла при корректном IP
    else:
        print('Неправильный IP-адрес')
